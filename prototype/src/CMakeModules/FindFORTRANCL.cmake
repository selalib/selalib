find_path(FORTRANCL_INCLUDE_DIRS NAMES cl.mod
	HINTS ${FORTRANCL_ROOT}
	PATH_SUFFIXES include 
	DOC "PATH TO cl.mod")

find_library(FORTRANCL_LIBRARIES NAMES fortrancl
	HINTS ${FORTRANCL_ROOT}
	PATH_SUFFIXES lib
	DOC "PATH TO libfortrancl.a")

FIND_LIBRARY(OPENCL_LIBRARIES OpenCL DOC "OpenCL lib")

if (FORTRANCL_INCLUDE_DIRS AND FORTRANCL_LIBRARIES AND OPENCL_LIBRARIES)
  set(FORTRANCL_FOUND YES)
  include_directories(${FORTRANCL_INCLUDE_DIRS})
endif()
