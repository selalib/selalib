add_library(sll_utilities STATIC misc_utils.F90)
add_dependencies(sll_utilities sll_working_precision)
IF(NOT STDF95)
  add_executable(test_utilities unit_test.F90)
  target_link_libraries(test_utilities sll_utilities sll_working_precision)
ENDIF()
