#3_FOUN3_FOUND(PYTHON_EXECUTABLE NAMES python3 python3.3 DOC "python")

#FILE(GLOB TXT_FILES "${CMAKE_CURRENT_SOURCE_DIR}/*.txt")
#FILE(COPY ${TXT_FILES} DESTINATION ${CMAKE_BINARY_DIR})

#MACRO(LIST_LENGTH var)
#   SET(entries)
#   FOREACH(e ${ARGN})
#     SET(entries "${entries}.")
#   ENDFOREACH(e)
#   STRING(LENGTH ${entries} ${var})
#ENDMACRO(LIST_LENGTH)

#IF(PYTHON_EXECUTABLE)

#   SET(PYTHON3_FOUND ON)
	 
#   SET(TRANSLATE_PYTHON_SCRIPT_CONNECTIVITY 
#      "../../python_scripts/translate_connectivity_info.py")

#   FILE(GLOB IEN_TXT_FILES RELATIVE "${CMAKE_CURRENT_SOURCE_DIR}" "${CMAKE_CURRENT_SOURCE_DIR}/IEN_*.txt")
#   FILE(GLOB LM_TXT_FILES RELATIVE "${CMAKE_CURRENT_SOURCE_DIR}" "${CMAKE_CURRENT_SOURCE_DIR}/LM_*.txt")
#   SET( ID_TXT_FILES "${CMAKE_CURRENT_SOURCE_DIR}/ID.txt")

   #FOREACH(arg  ${IEN_TXT_FILES})
   #	SET(IEN_FILES "${IEN_FILES} ${arg}")
   #ENDFOREACH(arg ${IEN_TXT_FILES})

#   LIST_LENGTH(length ${IEN_TXT_FILES})
   #MESSAGE("length: ${length}")

   #FOREACH(arg  ${LM_TXT_FILES})
   # 	SET(LM_FILES "${LM_FILES} ${arg}")
   # ENDFOREACH(arg ${LM_TXT_FILES})

   #MESSAGE("result separation ${bar}")

#   EXECUTE_PROCESS(
#	COMMAND ${TRANSLATE_PYTHON_SCRIPT_CONNECTIVITY} square_4p_n10 ${length} ${ID_TXT_FILES} ${IEN_TXT_FILES} ${LM_TXT_FILES})

#ELSE(PYTHON_EXECUTABLE)

#   MESSAGE(STATUS "Multipatch test needs python3")
#   SET(PYTHON3_FOUND OFF)

#ENDIF(PYTHON_EXECUTABLE)


#FILE(GLOB NML_FILES "${CMAKE_CURRENT_SOURCE_DIR}/*.nml")
#FILE(COPY ${NML_FILES} DESTINATION ${CMAKE_BINARY_DIR})


