Directory structure
===================
   test_2d_af_curvilinear         -- executable
   afsim2d_curvilinear_sdf.nml    -- input file
   README                         -- info (this file)
   run/                           -- simulation directory

HOWTO
=====
0. Create simulation directory if missing, and move into it
     $ mkdir run
     $ cd run

1. Run simulation within directory
     $ ../test_2d_af_curvilinear ../afsim2d_curvilinear_sdf

2. Open *.xmf database with Visit (*)


(*) Make sure that the Xdmf plugin is installed!

