Directory structure
===================
   test_guiding_center_2D_generalized_coords   -- executable
   README                                      -- info (this file)
   run/                                        -- output directory

HOWTO
=====
0. Create output directory if missing, and move into it
     $ mkdir run
     $ cd run

1. Run simulation within directory
     $ ../test_guiding_center_2D_generalized_coords

2. Open *.xmf database with Visit (*)


(*) Make sure that the Xdmf plugin is installed!

